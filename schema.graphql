extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.3", import: ["@key"])

type Query {
  reviewsForRecipe(recipeId: ID!): [RecipeReview!]!
}

type RecipeReview {
  id: ID!
  recipeId: ID!
  content: String!
}
